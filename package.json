{
  "name": "job-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Job application tracker — NestJS backend + Next.js frontend",
  "scripts": {
    "prepare": "husky",
    "check:floor": "node scripts/floor-guard.mjs",
    "check:migrations": "node scripts/migration-guard.mjs",
    "check:coverage-diff": "node scripts/coverage-diff.mjs --warn"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
