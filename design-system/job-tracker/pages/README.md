# Page overrides

One file per page, named after the route: `dashboard.md`, `jobs.md`, `jobs-detail.md`, `companies.md`, `profile.md`, `login.md`.

Rules here override `../MASTER.md` for that page only. If no file exists for a page, MASTER applies in full.
